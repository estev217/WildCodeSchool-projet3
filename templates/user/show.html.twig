{% extends 'layout.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header text-center">
                        <h1>Profil utilisateur</h1>
                    </div>
                        <table class="table text-white">
                            <tbody>

                                <tr>
                                    <th>Prénom</th>
                                    <td>{{ user.firstname }}</td>
                                </tr>
                                <tr>
                                    <th>Nom</th>
                                    <td>{{ user.lastname }}</td>
                                </tr>
                                <tr>
                                    <th>Email</th>
                                    <td>{{ user.email }}</td>
                                </tr>
                                <tr>
                                    <th>Rôles</th>
                                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                                </tr>
                                <tr>
                                    <th>Image</th>
                                    <td>{{ user.picture }}</td>
                                </tr>
                                <tr>
                                    <th>Mentor</th>
                                    <td>{{ user.mentor }}</td>
                                </tr>
                                <tr>
                                    <th>Référent</th>
                                    <td>{{ user.referent }}</td>
                                </tr>
                                <tr>
                                    <th>Date d'entrée</th>
                                    <td>{{ user.startDate ? user.startDate|date ("d/m/Y") : '' }}</td>
                                </tr>
                            </tbody>
                        </table>
                    <div class="d-flex align-items-end">
                        <button class="btn btn-primary mr-4 ml-4 mb-4">
                            <a class="text-white text-decoration-none" href="{{ path('user_edit', {'id': user.id}) }}">Modifier</a>
                        </button>
                    <div class="mb-4">
                    {{ include('user/_delete_form.html.twig') }}
                    </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="d-flex justify-content-center mb-3">
            <button class="btn btn-primary">
                <a class="text-white text-decoration-none" href="{{ path('user_index') }}">Retour</a>
            </button>
        </div>
    </div>
{% endblock %}
