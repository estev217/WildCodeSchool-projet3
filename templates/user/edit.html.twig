{% extends 'layout.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="container">
        <h1>Modifier un profil</h1>

        <div class="text-white">{{ include('user/_form.html.twig', {'button_label': 'Mettre à jour'}) }}</div>

        <a href="{{ path('user_index') }}">Retour à la liste</a>

        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
