{{ form_start(form) }}
<div class="m-3">
    <div class="row">
        <div class="col-md-4">{{ form_row ( form.firstname ) }}</div>
        <div class="col-md-4">{{ form_row ( form.lastname ) }}</div>
        <div class="col-md-4">{{ form_row ( form.picture ) }}</div>
    </div>
    <div class="row">
        <div class="col-md-4">{{ form_row ( form.email ) }}</div>
        <div class="col-md-4">{{ form_row ( form.password ) }}</div>
    </div>
    <div class="row">
        <div class="col-md-4">{{ form_row ( form.mentor ) }}</div>
        <div class="col-md-4">{{ form_row ( form.referent ) }}</div>
        <div class="col-md-4">{{ form_row ( form.manager ) }}</div>
    </div>
    <div class="row">
        <div class="col-md-4">{{ form_row ( form.position ) }}</div>
        <div class="col-md-4">{{ form_row ( form.role ) }}</div>
    </div>
    <div class="row">
        <div class="col-md-6">{{ form_row ( form.residence ) }}</div>
        <div class="col-md-6">{{ form_row ( form.residencePilote ) }}</div>
    </div>
    <div class="row">
        <div class="col-md-6">Date d'entrée{{ form_row ( form.startDate ) }}</div>
    </div>
</div>
    {{ form_widget(form) }}
    <button class="btn btn-primary text-white">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
