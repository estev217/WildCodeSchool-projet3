{% extends 'layout.html.twig' %}

{% block title %}Métier{% endblock %}

{% block body %}
    <h1>Métier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ position.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('position_index') }}">Retour</a>

    <a href="{{ path('position_edit', {'id': position.id}) }}">Modifier</a>

    {{ include('position/_delete_form.html.twig') }}
{% endblock %}
