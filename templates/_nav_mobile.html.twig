<div class="pos-f-t">
    <div class="collapse" id="navbarToggleExternalContent">
        <div class="p-1 nav-color">
            <p class="font-weight-bold text-uppercase text-center px-2 small py-2 mb-0">Mon Welcome Pack</p>

            <ul class="nav flex-row mb-0 justify-content-center">
                {% if is_granted('ROLE_COLLABORATOR') %}
                    <li class="nav-item">
                        <a href="{{ path('profile', {'user': app.user.id }) }}" class="nav-link">
                            <i class="fas fa-user mr-1 fa-fw"></i>
                            Mon profil
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('timeline', {'user': app.user.id }) }}#active" class="nav-link">
                            <i class="fas fa-walking mr-1 fa-fw"></i>
                            Parcours
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('checklist_item_checklist', {'user': app.user.id }) }}" class="nav-link">
                            <i class="fas fa-tasks mr-1 fa-fw"></i>
                            Checklist
                        </a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a href="{{ path('toolbox') }}" class="nav-link">
                        <i class="fas fa-toolbox mr-1 fa-fw"></i>
                        Boîte à outils
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('info') }}" class="nav-link">
                        <i class="fas fa-cubes mr-1 fa-fw"></i>
                        Nemea
                    </a>
                </li>
            </ul>

            {% if is_granted('ROLE_MANAGER') %}
                <p class="font-weight-bold text-uppercase text-center px-2 small pt-2 pb-2 mb-0">Le coin du
                    manager</p>

                <ul class="nav flex-row mb-0 justify-content-center">
                    <li class="nav-item">
                        <a href="#" class="nav-link">
                            <i class="fas fa-clipboard-list mr-1 fa-fw"></i>
                            Checklist
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('manager_show', {'user': app.user.id }) }}" class="nav-link">
                            <i class="fas fa-users mr-1 fa-fw"></i>
                            Collaborateurs
                        </a>
                    </li>
                </ul>
            {% endif %}
            <br>
            <ul class="nav flex-row mb-0 justify-content-end">
                <li class="nav-item">
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}" class="nav-link">
                            <i class="fas fa-sign-out-alt mr-1 fa-fw"></i>
                            Déconnexion
                        </a>
                    {% endif %}
                </li>
            </ul>
        </div>
    </div>
    <nav class="navbar navbar-dark nav-color">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarToggleExternalContent" aria-controls="navbarToggleExternalContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div>
            <p class="text-uppercase">menu</p>
        </div>
    </nav>
</div>
