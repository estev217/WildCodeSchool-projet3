<form method="post" action="{{ path('checklist_item_delete', {'id': checklist_item.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ checklist_item.id) }}">
    <button class="btn text-primary">Supprimer</button>
</form>
