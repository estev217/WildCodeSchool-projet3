{% extends 'layout.html.twig' %}

{% block title %}Edit ChecklistItem{% endblock %}

{% block body %}
    <div class="container">
        <h1>Éditer la checklist</h1>

        <div class="text-white">{{ include('checklist_item/_form.html.twig', {'button_label': 'Mettre à jour'}) }}</div>

        <a href="{{ path('checklist_item_index') }}">Retour à la liste</a>

        {{ include('checklist_item/_delete_form.html.twig') }}
    </div>
{% endblock %}
