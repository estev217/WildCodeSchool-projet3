{% extends 'layout.html.twig' %}

{% block title %}New ChecklistItem{% endblock %}

{% block body %}
    <div class="container">
    <h1>Créer un nouvel élément checklist</h1>

    <div class="text-white">{{ include('checklist_item/_form.html.twig') }}</div>

    <a href="{{ path('checklist_item_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
