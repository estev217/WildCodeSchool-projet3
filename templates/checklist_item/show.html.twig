{% extends 'base.html.twig' %}

{% block title %}ChecklistItem{% endblock %}

{% block body %}
    <h1>ChecklistItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ checklist_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ checklist_item.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ checklist_item.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('checklist_item_index') }}">back to list</a>

    <a href="{{ path('checklist_item_edit', {'id': checklist_item.id}) }}">edit</a>

    {{ include('checklist_item/_delete_form.html.twig') }}
{% endblock %}
