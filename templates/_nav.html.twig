<div class="navigation vertical-nav" id="sidebar">
    <div class="nav-content">
        <div class="cornerlogo">
            <div class="logo-circle">
                <img src="{{ asset('build/images/logo.png') }}" alt="logo">
            </div>
        </div>
        <div class="menus">
            <ul class="nav flex-column mb-0 pb-3">
                <li class="nav-item">
                    {% if app.user %}
                        <a href="{{ path('app_logout') }}" class="nav-link ml-3">
                            Déconnexion
                            <i class="fas fa-sign-out-alt fa-fw"></i>
                        </a>
                    {% endif %}
                </li>
            </ul>

            <p class="font-weight-bold text-uppercase text-center px-2 small pt-3 pb-3 mb-0">
                <a class="nav-title" data-toggle="collapse"
                   href="#collapse-categories" role="button" aria-expanded="false" aria-controls="collapse-categories">
                    Articles<i class="fas fa-sort-down ml-2"></i>
                </a>
            </p>

            <div class="collapse" id="collapse-categories">
                {{ render ( controller (
                    'App\\Controller\\CategoryController::navigation'
                )) }}
            </div>
            {% if is_granted('ROLE_COLLABORATOR') %}
                <p class="nav-title font-weight-bold text-uppercase text-center px-2 small pt-4 pb-3 mb-0">Mon Welcome
                    Pack</p>

                <ul class="nav flex-column mb-0">
                    <li class="nav-item">
                        <a href="{{ path('timeline', {'user': app.user.id }) }}#active" class="nav-link">
                            <i class="fas fa-walking mr-3 fa-fw"></i>
                            Parcours
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('checklist_item_checklist', {'user': app.user.id }) }}" class="nav-link">
                            <i class="fas fa-tasks mr-3 fa-fw"></i>
                            Checklist
                        </a>
                    </li>
                </ul>
            {% endif %}

            {% if is_granted('ROLE_MANAGER') %}
                <p class="nav-title font-weight-bold text-uppercase text-center px-2 small pt-4 pb-3 mb-0">Le coin du
                    manager</p>

                <ul class="nav flex-column mb-0">
                    <li class="nav-item">
                        <a href="{{ path('manager_show', {'user': app.user.id }) }}" class="nav-link">
                            <i class="fas fa-users mr-3 fa-fw"></i>
                            Collaborateurs
                        </a>
                    </li>
                </ul>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') %}
                <p class="nav-title font-weight-bold text-uppercase text-center px-2 small pt-4 pb-3 mb-0">
                    Administrateur</p>
                <ul class="nav flex-column mb-0">
                    <li class="nav-item">
                        <a href="{{ path('admin') }}" class="nav-link">
                            <i class="fas fa-user-cog mr-3 fa-fw"></i>
                            Gestion
                        </a>
                    </li>
                </ul>
            {% endif %}
        </div>
    </div>
</div>
