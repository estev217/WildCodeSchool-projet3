{% extends 'layout.html.twig' %}

{% block title %}Edit Content{% endblock %}

{% block body %}
    <div class="container">
    <h1>Modifier un contenu</h1>

        <div class="text-white">{{ include('content/_form.html.twig', {'button_label': 'Mettre à jour'}) }}</div>

    <a href="{{ path('content_index') }}">Retour à la liste</a>

    {{ include('content/_delete_form.html.twig') }}
    </div>
{% endblock %}
