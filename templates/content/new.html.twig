{% extends 'layout.html.twig' %}

{% block title %}New Content{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-12">
                <div class="card w-100 m-auto">
                    <div class="card-header text-center">
                        <h1>Créer du nouveau contenu</h1>
                    </div>
                    <div class="m-3">
                        {{ include('content/_form.html.twig') }}
                        <a href="{{ path('image_index') }}" target="_blank">Voir les images disponibles</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="d-flex justify-content-md-center mt-3 mb-3">
        <a class="btn btn-primary" href="{{ path('content_index') }}">Retour</a>
    </div>
{% endblock %}
